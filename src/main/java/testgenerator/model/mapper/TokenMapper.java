package testgenerator.model.mapper;

import org.keycloak.representations.AccessTokenResponse;
import testgenerator.model.dto.TokenDto;

public class TokenMapper {

//    public static TokenDto tokenDto(AccessTokenResponse response) {
//        return new TokenDto(response.getToken(), response.getExpiresIn(), response.getRefreshToken(), response.getRefreshExpiresIn());
//    }
}
